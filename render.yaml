services:
  - type: web
    name: crypto-intel-bot
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    nodeVersion: 22
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
